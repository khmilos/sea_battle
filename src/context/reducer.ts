import { ContextState, ContextActionTypes } from './types';

function reducer (state: ContextState, action: ContextActionTypes) {
  switch (action.type) {
    default: return state;
  }
}

export default reducer;
